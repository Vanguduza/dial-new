import { hashObject } from './knowledge-graph-core.mjs';
export function projectDesignAuthority({unitMap,designMode='NEW_DIAL_DESIGN',lockedBaseline=null,donorTransformationHash=null}={}){if(!unitMap?.product_experience_map?.applicable)throw new Error('Product Experience knowledge required for design projection');const base={schema_version:1,unit_lineage_id:unitMap.unit_lineage_id,unit_revision_hash:unitMap.unit_revision_hash,authority_refs:unitMap.product_experience_map.design_authorities||unitMap.design_authorities||[],product_experience_knowledge_hash:unitMap.product_experience_map.knowledge_hash,design_mode:designMode,locked_baseline:lockedBaseline,donor_transformation_hash:donorTransformationHash,prohibited_patterns:['FAKE_DATA','PLACEHOLDER_HELPER_TEXT','DEAD_CONTROLS','GENERIC_AI_UI','DONOR_BRAND_AUTHORITY','RAW_TECHNICAL_IDS'],authority:'DERIVED_DESIGN_PROJECTION'};return{...base,projection_hash:hashObject(base)};}
export function renderDesignMarkdown(p){return[`# DIAL Design Authority Projection`,`Generated projection — not design authority.`,`Unit: ${p.unit_lineage_id}@${p.unit_revision_hash}`,`Projection: ${p.projection_hash}`,`Mode: ${p.design_mode}`,`Product Experience: ${p.product_experience_knowledge_hash}`,`Prohibited: ${(p.prohibited_patterns||[]).join(', ')}`].join('\n')+'\n';}
