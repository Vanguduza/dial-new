import { hashObject } from './knowledge-graph-core.mjs';
const SECRET=/(?:-----BEGIN [A-Z ]*PRIVATE KEY-----|\b(?:api[_-]?key|secret|password|token)\b\s*[:=]\s*["']?[^\s"']{8,})/ig;const RESTRICTED=/\b(customer|patient|claimant)\b.{0,30}\b(email|phone|address|diagnosis|claim|payment)\b/i;
export function sanitizeAndClassifyProviderInput(input,{requestedClass='INTERNAL_SAFE_FOR_APPROVED_PROVIDER'}={}){let text=String(input??'');const findings=[];text=text.replace(SECRET,(m)=>{findings.push('SECRET_REDACTED');return '[REDACTED_SECRET]'});const restricted=RESTRICTED.test(text);const data_class=restricted?'RESTRICTED':requestedClass;if(data_class==='RESTRICTED'||data_class==='SECRET')return{ok:false,data_class,reason:'PROVIDER_EGRESS_DENIED',findings,sanitized_hash:hashObject(text)};return{ok:true,data_class,sanitized:text,findings,sanitized_hash:hashObject(text)};}
