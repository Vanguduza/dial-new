export class StitchAdapter{constructor({transport,enabled=false,projectId=null}={}){this.transport=transport;this.enabled=enabled;this.projectId=projectId;}async health(){if(!this.enabled)return{state:'DISABLED'};if(typeof this.transport?.health!=='function')return{state:'UNAVAILABLE'};return this.transport.health();}async generate(input){if(!this.enabled)throw new Error('STITCH_DISABLED');if(typeof this.transport?.generate!=='function')throw new Error('STITCH_TRANSPORT_UNAVAILABLE');if(!input?.design_projection_hash)throw new Error('DESIGN_AUTHORITY_PROJECTION_REQUIRED');return this.transport.generate({...input,project_id:this.projectId});}}
