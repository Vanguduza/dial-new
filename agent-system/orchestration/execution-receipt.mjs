import { DEFAULT_CONTROL_HOME,writeJsonAtomic } from './state-store.mjs';import { hashObject } from './knowledge-graph-core.mjs';
const SECRET=/(?:sk-[A-Za-z0-9_-]{16,}|-----BEGIN [A-Z ]*PRIVATE KEY-----|(?:password|token|secret|api[_-]?key)\s*[:=]\s*["']?[A-Za-z0-9_\-\.]{12,})/i;function now(){return new Date().toISOString();}
export function buildDevelopmentExecutionReceipt(input={}){const base={schema_version:1,receipt_version:'dial-aef-receipt-1',state:input.state||'EXECUTION_COMPLETED',task:input.task||{},unit:input.unit||{},authority:input.authority||{},routing:input.routing||{},compute:input.compute||{},topology:input.topology||{},context:input.context||{},leases:input.leases||[],results:input.results||[],verification:input.verification||{},gates:input.gates||[],integration:input.integration||{admitted:false},created_at:input.created_at||now()};if(SECRET.test(JSON.stringify(base)))throw new Error('RECEIPT_SECRET_DETECTED');const receipt_hash=hashObject({...base,created_at:null});return{...base,receipt_id:`DER-${receipt_hash.slice(0,32)}`,receipt_hash};}
export function persistDevelopmentExecutionReceipt({root=DEFAULT_CONTROL_HOME,receipt}={}){if(!receipt?.receipt_hash||buildDevelopmentExecutionReceipt({...receipt,created_at:receipt.created_at}).receipt_hash!==receipt.receipt_hash)throw new Error('invalid execution receipt hash');writeJsonAtomic(`execution/receipts/${receipt.receipt_id}.json`,receipt,root);if(receipt.task?.task_id)writeJsonAtomic(`execution/tasks/${receipt.task.task_id}/receipt-pointer.json`,{receipt_id:receipt.receipt_id,receipt_hash:receipt.receipt_hash},root);return receipt;}
